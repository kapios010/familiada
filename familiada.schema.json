{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Familiada",
  "description": "Zestaw rund do familiady",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "pytanie": { "type": "string", "minLength": 1 },
      "mnożnik": { "type": "integer", "minimum": 1, "maximum": 3 },
      "odpowiedzi": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "treść": { "type": "string", "minLength": 1, "maxLength": 17 },
            "punkty": { "type": "integer", "minimum": 1, "maximum": 100 }
          },
          "maxItems": 6,
          "minItems": 1
        }
      }
    }
  },
  "maxItems":5,
  "minItems":1
}
